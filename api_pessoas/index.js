const express = require("express");

const app = express();

const pessoas = [];

// Rota para cadastro de pessoas
pessoas.push();
// Rota para consulta de pessoas
pessoas
// Rota para consultar pessa por nome
const retorno = pessoas.find(()=> { i.nome === nome});

// Rota para deletar pessoa


app.listen(8080,()=>{
    console.log("O servidor está rodando na porta 8080");
});